export function LeadText({ text }: { text: string }) {
  // ----- global states -----

  // ----- local states -----

  return <p className="text-[2vh] font-normal leading-[2.314815vh] text-[#585858]">{text}</p>;
}
