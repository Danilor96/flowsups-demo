import { Important } from '&/icons/Icons';

export function ManagerTaskIcon() {
  // ----- global states -----

  // ----- local states -----

  return (
    <div className="w-fit h-fit flex justify-center items-center px-[0.3vw] py-[0.95vh]  mx-auto rounded-full bg-[#C9EBE6]">
      <Important />
    </div>
  );
}
