export function Body({ children }: { children: React.ReactNode }) {
  // ----- global states -----

  // ----- local states -----

  return (
    <section className="relative w-full h-fit bg-[#DEF2FF] pt-[1.018519vh] pl-[0.885417vw] pr-[0.885417vw] pb-[1.388889vh] rounded-[0.520833vw] mb-[2vh]">
      {children}
    </section>
  );
}
